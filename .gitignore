# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# My files
.env

# Real broker snapshot. Belongs in $CASH_ASH_HOME, never here, but ignore
# it in the project root too in case a copy lands during setup. Anchored: an
# unanchored pattern also matched tests/fixtures/seed_snapshot.toml, silently
# excluding the synthetic fixture test_seed.py reads and breaking a fresh clone.
/seed_snapshot.toml
*.db
*.db-wal
*.db-shm

# Built public site (marketing/build.py)
_site/
